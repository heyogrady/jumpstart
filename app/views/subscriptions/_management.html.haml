- if subscription.owner?(current_user)
  - if subscription.active? && !subscription.scheduled_for_cancellation?
    %p= link_to(t("subscriptions.change_plan"), edit_subscription_path)
    %p= link_to(t("subscriptions.cancel"), new_subscriber_cancellation_path, class: "cancel")
